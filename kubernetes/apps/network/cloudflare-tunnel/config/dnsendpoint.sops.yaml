---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: cloudflare-tunnel
spec:
  endpoints:
    - dnsName: "external.example.com"
      recordType: CNAME
      targets: ["fake.cfargotunnel.com"]
